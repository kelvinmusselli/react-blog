import React from 'react';
import { Container, Logo, Title } from './styles';

const Header = () => {
  return (
    <Container>
      <Logo>
        <Title>Blog</Title>
      </Logo>
    </Container>
  );
};

export default Header;
